- **Refactor: Drop redundant `get_type_of_module` call in `imported.impl.jac`**: The non-directory branch of `get_type_of_module_item` re-fetched the from-clause module type that the outer gate condition had already resolved. The duplicate call now reuses the existing `from_mod_type` binding. No behavior change.
